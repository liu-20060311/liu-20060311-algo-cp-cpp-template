#include <bits/stdc++.h>
using namespace std;
using ll = long long;

// Quick debug macro: define LOCAL to enable
#ifdef LOCAL
  #define dbg(x) cerr << __FILE__ << ":" << __LINE__ << " " << #x << " = " << (x) << "\n"
#else
  #define dbg(x) do {} while(0)
#endif

// Helper aliases
#define all(x) (x).begin(), (x).end()

void solve_case() {
    // 处理单个测试用例的逻辑，读取输入并输出结果
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int T = 1;
    if (!(cin >> T)) return 0; // 处理无输入时退出
    while (T--) {
        solve_case();
    }
    return 0;
}
